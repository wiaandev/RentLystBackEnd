version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: E!1vmpd
      POSTGRES_DB: rentro-database
    ports:
      - "5433:5432"  # Adjusted to avoid port conflict
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
